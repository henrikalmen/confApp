# Project State

Last Updated: 2026-08-29 23:59

## Current Phase
<!-- Active phase/milestone name and one-line status -->

Phase: Session Activities: complete. All five stories (S01-S05) done and verified, final gap review PASS, ADR-007 accepted and implemented, and the reconciliation ledgers swept to 3 open entries - all three carrying a recorded owner override. Committed 2026-08-29 as ab61f59. The conference-setup-and-schedule plan still governs its own tail: S11 partially executed, S13 deferred, S12 unreachable behind it.
Status: On Track – All five session-activities stories are done and verified, and the working tree is committed clean at ab61f59. Build clean, 1201/1201 tests across 73 files (three consecutive runs), typecheck and lint clean; format:check fails only on three long-standing unrelated files (api/test/join-code.test.ts, visual/conferences.spec.ts, web/src/components/JoinCodePanel.tsx). The final gap review returned PASS - Functionality 7/10 on its threshold, Completeness 9/10, Wiring 8/10 - and its report is docs/specs/session-activities/session-activities-gap-review-claude-2026-08-29.md. RECONCILIATION LEDGERS: 30 entries across the five stories, now 23 CLOSED, 4 WITHDRAWN, 3 OPEN, 0 RECONCILE REQUIRED. Every one of the 13 fixes was proved red before green. The 3 still OPEN are deliberate deferrals, each carrying a recorded owner Override reason, so nothing blocks the completion-presentation gate: tightening a membership guard S08 owns; discarding a Conference's queued Post-its when a Member leaves it; and a device-wide held-items surface so a returned-to-author Post-it is visible outside its own Session panel. The last two are new scope beyond S04's contract. DECISION RECORDS: ADR-007 (Accepted) removes the ballot trigger so a Vote advances no cursor at all - the Session-scoped watermark was a noiseless vote-arrival oracle to any Member on an unthrottled endpoint, and the earlier opaque-counter change addressed what the value said but not when it moved. ADR-006 was amended: its Decision 1 now models correlation across successive responses and against out-of-band observation, and names the Facilitator's live-tally correlation as an accepted open channel inherent to US07 rather than an oversight. KNOWN STALE, not blocking but misleading to a reader: roughly 33 references to the old cursor name activity_watermark_at survive in the S01, S02, S03 and S05 FIS bodies, in two reconciliation-ledger Notes, and in plan.json#sharedDecisions. The cursor is now activity_watermark, a bigint counter. plan.json content may only be rewritten by andthen:plan, and a full regeneration risks the preservation predicate resetting completed stories to pending, so that one needs a deliberate decision rather than a sweep. RESPONSIVENESS GATE NOW CLOSED (2026-08-29): the visual suite ran end to end for the first time this session - 83/83 across all 11 specs at 375/768/1280 against the composed stack, on freshly rebuilt web and api images with the database already level. The earlier blocker was not a missing Docker: the engine is in the `Ubuntu` WSL2 distro and was never on the Windows PATH, which is the same misread that produced the erroneous S13 hold on 2026-08-20. All four session-activities stories are covered by name - S01 round list, run controls and authoring form; S02 board, compose and named post-its; S03 poll options, submit, voted and results including a closed-round tally; S04 pending, late and returned post-its - so the standing three-width acceptance criterion is met for everything S01-S04 added. STILL OUTSTANDING: /code-review ultra has never been run on this branch, which STATE.local.md has flagged since before this run and which is now worth doing against a green committed baseline.

## Active Stories
<!-- When a plan.json governs (has undone stories), Active Stories derive from it on read – store rows only for ad-hoc work in no governing plan.
     Otherwise one row per in-progress story (Owner = who is executing it); move completed stories to Recently Completed. -->

| Story | Owner | Status | FIS | Notes |
|-------|-------|--------|-----|-------|
| _none_ | | | | |

## Recently Completed
<!-- Last 2 milestones only, one line each. Older milestones belong in CHANGELOG.md.
     If more exist, add a trailing line: "Previous: 0.3, 0.2, 0.1" -->

- **Init** (2026-08-16): AndThen workflow structure scaffolded – agent instruction files, core orientation docs, planning docs, GitHub issue-tracker config.
- **Product clarification** (2026-08-16): product-mode clarify run – `docs/PRODUCT.md` written from four rounds of Discovery & Ideation.

## Blockers
<!-- Anything preventing progress. Remove resolved blockers and those older than ~14 days with no activity. -->

- **S13 deferred on three signed execution holds** (2026-08-20, preflight verdict DEFERRED, `plan.json` status `blocked`; corrected 2026-08-21). No provisioned deploy target (intended: Azure Container Apps); no registry (intended: Azure Container Registry); no deployed PostgreSQL (intended: Azure Database for PostgreSQL, TI10's managed path). A fourth hold, `container-build-toolchain`, was recorded in error – Docker Engine was present in the `Ubuntu` WSL2 distro all along and image builds succeed; that hold is being cleared separately against real build evidence. All intended targets are recorded as *intended for the next run only* – neither `docs/DECISIONS.md` → Pending entry is closed. Full detail in the FIS's `## Deferred Decisions` section.
- **S12 unreachable until S13 lands** – it `dependsOn` S13 and its p95 and 100-concurrent thresholds are unfalsifiable without a deployed environment to measure against.
- **S11 blocked on hardware, not on code** – TI02–TI11 need a physical Android device, and every iOS criterion needs macOS + Xcode + a registered device. TI04 additionally needs two Google OAuth clients registered in Google Cloud Console and a running API. A simulator does not satisfy a criterion that names a device (S11 Testing Strategy).
- **Production database hosting** – deferred to phase 2 (ADR-003). Not blocking development.
- **Push service not confirmed** – Azure Notification Hubs recommended, fronting APNs + FCM.

## Recent Decisions
<!-- Key decisions made in the last 1-2 sessions. Keep max ~10. Move older items to ADRs. -->

- **Session Activities plan created** (2026-08-28): 5 stories, 3 phases, from `docs/specs/session-activities/prd.md`. S01 Round authoring and lifecycle is the enabler; S02 named post-it contribution and S03 anonymous poll voting run in parallel after it; S04 offline queueing and S05 contribution-safe session deletion harden. S03 carries the plan's only high risk – vote anonymity is a storage-level guarantee, so its acceptance is a property of the schema and a passing UI proves nothing. One PRD interpretation is flagged for confirmation before S01 executes: a Poll's question text is frozen on the same trigger as its options, which the decision did not explicitly cover.
- **Correction (2026-08-21): the "no container image builder" finding from 2026-08-20 was wrong.** Docker Engine 29.1.3 was running in the `Ubuntu` WSL2 distro throughout, with the confApp composed stack up since 2026-08-16/17. The probe that produced the finding checked only Windows-side locations and the `docker-desktop` distro, never the `Ubuntu` one – despite that distro having been observed as Running in the same session and then recommended as the install target. Contradicting evidence was in hand and not reconciled: the same session established that the PostgreSQL integration tests were genuinely executing against a real database, which had to be running somewhere. S13's other three holds (no deploy target, no registry, no deployed PostgreSQL) are unaffected and remain valid, so S13 stays `blocked` – the correction changes the count and the reason, not the outcome. Separately, this explains the S09 note about the SPA container's proxy returning 502 locally: `confapp-api-1` has been crash-looping since it started because `.env`'s `GOOGLE_AUDIENCE_ALLOWLIST` still contains the literal placeholder `<paste client ID>`, and S02's startup guard correctly refuses to boot on a non-literal entry. The proxy has no upstream; it is not misconfigured.
- P3 Hardening **started and immediately contained** – superseding the S10 entry below, which correctly recorded P3 as not started at the time it was written. An exec-plan run scoped to S11 + S13 completed with **zero stories done**, by design. Preflight on S13 returned DEFERRED: its blocking decisions were missing exec-time inputs rather than design questions – no provisioned deploy target, no registry, no deployed PostgreSQL – and the owner signed off deferring rather than provisioning mid-run. (A fourth hold, a missing container image builder, was recorded here in error and corrected 2026-08-21 – see the correction entry above.) S13 is now `blocked` with signed `#### DEFERRED DECISION:` blocks naming Azure Container Apps, Azure Container Registry and Azure Database for PostgreSQL as *intended targets for the next run only*; neither `docs/DECISIONS.md` → Pending entry is closed, and the ADR sweep deliberately authored nothing because S13's FIS is engineered to execute without closing them (OC02). S11 ran as an approved partial – **TI01 only**, fully verified, staying `spec-ready` at 1 of 31 checkboxes; TI05 was deliberately not authored because Swift/Kotlin cannot be compiled on this machine, and TI11's web half was skipped as already covered by S09/S10. Gates green: typecheck, lint, build, 815/815 tests across 50 files (2026-08-20).
- S10 (Offline schedule access) done → **P2 Feature slices complete** (S03–S10). Closed on orchestrator verification re-run independently of the worker's self-report: 805/805 tests across 50 files (baseline 740/46), typecheck + lint + build clean, the new offline tests confirmed genuinely executing via `--reporter=verbose`, `visual/offline-schedule.spec.ts` 9/9 at 375/768/1280 against the Vite dev server, and every Structural Criterion spot-checked in source – exactly one `diffSchedule` (S09's, consumed not duplicated), no outbox/queue/replay path anywhere, the anchor pair persisted rather than the derived offset, staleness as elapsed age with zero date-conversion calls in `web/src/offline/`, the reconnect cursor reading `conference.lastUpdatedAt`, cache keyed on (`sub`, conferenceId) with no email in the offline layer, and the service worker precaching only `['/', '/index.html', '/config.js']` plus fingerprinted assets with `/api/` bypassed. This exec-plan run was deliberately scoped to S10 and stops here – P3 is **not** started (2026-08-20).
- S03 (Conference lifecycle) reconciled to **done** – its code shipped in `d54cbf9` and S04–S09 all built on it, but `plan.json` still read `spec-ready` and all 29 FIS checkboxes were unchecked. Status and checkboxes were corrected against the shipped evidence (740/740 suite green; 34 integration tests against real PostgreSQL, all 10 Structural Criteria covered, TI11/TI12 covered by `ConferencesPanel.test.tsx` and 9 visual specs at 375/768/1280). Nothing was re-implemented – this was bookkeeping catching up with the code (2026-08-20).
- S09 closed on a raised evidence standard: a Structural Criterion is earned by a **behavioural** assertion, not by a source grep alone. The grep for "no timezone conversion in the refresh path" listed four files and omitted `schedule-view-model.ts` – the module that formats every Session time an Attendee reads – so a `toLocaleTimeString` there passed it untouched. The two-timezone render test now proves the property; the grep is kept as the cheap guard and its file list corrected (2026-08-19).
- S05: joinability reason-naming (`joinRefusalReason` / `assertJoinable`) added to S03's `api/src/conferences/lifecycle.ts` in place rather than as a second mapping in S05 – keeps exactly one definition of joinability, as the FIS Constraints section directs. `isJoinable` now delegates to it, and S03's pre-existing lifecycle tests still pass unmodified, so the refactor is regression-guarded on behavioural equivalence (2026-08-17).
- Plan created: Conference setup & schedule (13 stories, 3 phases, all spec-ready) (2026-08-16).
- FR7 split: post-publish schedule editing planned; push notification deferred pending REQ-005 and the push delivery service decision – in-app propagation is the primary channel (2026-08-16).
- Foundation (scaffold, sign-in, Capacitor shells) planned inside the Conference setup & schedule bundle rather than a separate Phase 2 plan (2026-08-16).
- React SPA targeting browser + Android + iOS, responsive across viewports (2026-08-16).

<!-- Trimmed to the last 10 per the Recent Decisions maintenance rule; the trimmed entries are recorded in ADR-001 (Capacitor mobile packaging), ADR-002 (Google Workspace OIDC), ADR-003 (PostgreSQL), ADR-004 (API + SPA as container images, superseding serverless on Azure), `AGENTS.md` (offline scope, agent-instruction layout, issue tracker, post-its named / votes anonymous) and `docs/PRODUCT.md` (MVP slice, product clarification, internal-app scope / store presence / iOS push). -->
